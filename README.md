# react-exp13-login-redirect
To implement **page redirection** in React using **React Router** after a successful login.   When a user logs in with valid credentials, they are automatically redirected to the **Dashboard** page.
